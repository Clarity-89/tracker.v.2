<h1>All users</h1>

<%= will_paginate @users %>
<ul class="users">
  <%= render @users %>
</ul>
<%= will_paginate @users%>

<!-- Page-specific styles -->
<style>header, main, footer {
  padding-left: 240px;
}</style>