<h1>All users</h1>

<%= will_paginate %>
<ul class="users">
  <%= render @users %>
</ul>
<%= will_paginate %>

<!-- Page-specific styles -->
<style>header, main, footer {
  padding-left: 240px;
}</style>